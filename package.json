{
  "name": "@cloud-cli/proxy",
  "version": "0.0.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "certs": "openssl req -x509 -newkey rsa:4096 -sha256 -days 365 -nodes -keyout certs/privkey.pem -out certs/fullchain.pem -subj '/CN=example.com' -addext 'subjectAltName=DNS:example.com,DNS:*.example.com,IP:127.0.0.1'",
    "tdd": "jest --watchAll"
  },
  "prettier": "@cloud-cli/prettier-config",
  "repository": {
    "type": "git",
    "url": "https://github.com/cloud-cli/proxy.git"
  },
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "@cloud-cli/prettier-config": "^1.0.0",
    "@cloud-cli/jest-config": "^1.0.0",
    "@cloud-cli/typescript-config": "^1.0.0"
  }
}
